tasks:
  - shell:
      task:
        become: no
      vars:
        free_form: ls -lah
  - shell:
      task:
        become: no
        var-keys:
          - free_form
      vars:
        free_form: ls -lah /tmp
