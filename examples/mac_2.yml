- localhost:
    meta:
      type: host
    tasks:
      vars:
        freckles_repo:
          - repo: https://github.com/makkus/dotfiles-new.git
            #dest: ~/dotfiles
            #paths: ["minimal", "dev", "no-x", "x"]
            profiles: ["no-x"]
      tasks:
        - freckles
