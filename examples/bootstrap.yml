- localhost:
    meta:
      type: host
    vars:
      dotfiles:
        local: ~/dotfiles
        url: https://github.com/makkus/dotfiles-new.git
        paths: ["minimal", "dev", "no-x", "x"]

    tasks:
      - install-nix
      #- install-conda
      - checkout-git:
          repos:
            - dest: ~/.emacs.d
              repo: https://github.com/syl20bnr/spacemacs
            - dest: ~/.zplug
              repo: https://github.com/zplug/zplug
      - install:
          name:
          - zile
          - gawk
          #- https://releases.hashicorp.com/vagrant/1.9.4/vagrant_1.9.4_x86_64.deb


#      - dotfiles:
#          install: yes
#          stow: yes


