- localhost:
    meta:
      type: host
    vars:
      dotfile_repo:
        repo: https://github.com/makkus/dotfiles-new.git
        dest: ~/dotfiles
        profiles: ["minimal", "dev", "no-x", "x"]
    tasks:
      - freckles



