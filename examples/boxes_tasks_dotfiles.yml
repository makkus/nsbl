- localhost:
    meta:
      type: host
    vars:
      dotfile_repo:
        repo: https://github.com/makkus/dotfiles-new.git
        dest: ~/dotfiles
        paths: ["minimal", "dev", "no-x", "x"]
    tasks:
      - freckles



